"""Dataclasses for Match, Team, Player, Event with `.public()` views.

Populated in T1.1; shapes defined by docs/WEBSOCKET_PROTOCOL.md §3.
`.public()` must never include puzzle answers.
"""

from __future__ import annotations
